{
  "name": "simple-site",
  "version": "1.0.0",
  "description": "Простой статический сайт на HTML и CSS.",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "live-server --port=3000 --open=./index.html",
    "start": "live-server --open=./index.html",
    "serve": "live-server --open=./index.html",
    "install-deps": "npm install",
    "test": "vitest",
    "test:watch": "vitest watch"
  },
  
  "devDependencies": {
    "live-server": "^1.2.2",
    "vitest": "^4.1.5"
  }
}
